// HIDDEN — Subscriptions page disabled. To re-enable, restore from src/app/_disabled/subscriptions.page.tsx
import { redirect } from "next/navigation";

export default function SubscriptionsPage() {
  redirect("/");
}
