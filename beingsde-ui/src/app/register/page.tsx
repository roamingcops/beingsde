// HIDDEN — Register page disabled. To re-enable, restore from src/app/_disabled/register.page.tsx
import { redirect } from "next/navigation";

export default function RegisterPage() {
  redirect("/");
}
